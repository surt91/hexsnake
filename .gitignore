# Rust
/target

# trunk (WASM-Build-Ausgabe)
dist/

# Python (Phase 9)
python/.venv/
__pycache__/
*.pyc
# maturin-built extension (compiled into the editable install)
*.so
*.pyd
# uv lockfile pins platform-specific (CUDA) torch wheels; resolve per machine
python/uv.lock

# E2E
e2e/node_modules/
e2e/package-lock.json

# Editor/OS
.idea/
.vscode/
*.swp
.DS_Store

# data
training-out